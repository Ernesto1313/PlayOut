The data layer is ready (Instalacion.kt, InstalacionDao.kt, AppDatabase.kt).
Gradle syncs successfully. Now set up Hilt and the Repository layer.

Create the following files:

**1. `PlayOutApplication.kt`** at the root package `com.ernesto.playout`
- Annotated with @HiltAndroidApp
- Also update AndroidManifest.xml to register this Application class

**2. `di/AppModule.kt`**
- @Module @InstallIn(SingletonComponent::class)
- Provides AppDatabase using createFromAsset("playout.db")
- Provides InstalacionDao from the database
- Both bindings as @Singleton

**3. `data/repository/InstalacionRepository.kt`**
- Constructor-injected with InstalacionDao via @Inject
- Exposes the following suspend/flow functions:
  - getAllInstalaciones(): Flow<List<Instalacion>>
  - getInstalacionById(fid: Int): Flow<Instalacion?>
  - getInstalacionesByCategoria(categoria: String): Flow<List<Instalacion>>

Do not create any ViewModels or UI yet.
Do not run any gradle or build commands.